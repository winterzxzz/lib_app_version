<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Needed to read the Play Store listing. Merged into the host app automatically. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
